{% extends 'base.html.twig' %}

{% block title %}CalandarDay
{% endblock %}

{# oveeriding annoying header #}
{% block header %}{% endblock %}

{% block body %}
	<h1>CalandarDay</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ calandar_day.id }}</td>
			</tr>
			<tr>
				<th>Date</th>
				<td>{{ calandar_day.date ? calandar_day.date|date('Y-m-d') : '' }}</td>
			</tr>
			<tr>
				<th>DayStart</th>
				<td>{{ calandar_day.dayStart ? calandar_day.dayStart|date('H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>Today's Appointments:</th>

				<td>
					{% set time_slots = calandar_day.timeSlots %}
					{% if role == "ROLE_USER" %}
						{{ include( 'appointment/index.html.twig' , {'appointments': appointments  }   ) }}
					{% else %}
						{{ include( 'appointment/index.html.twig'  ) }}
					{% endif %}
				</td>
			</tr>
		</tbody>
	</td>
</tr>
<tr>
	<th>DayEnd</th>
	<td>{{ calandar_day.dayEnd ? calandar_day.dayEnd|date('H:i:s') : '' }}</td>
</tr>
<tr>
	<th>SessionDuration</th>
	<td>{{ calandar_day.sessionDuration }}</td>
</tr>
<tr>
	<th>LunchBreakStart</th>
	<td>{{ calandar_day.lunchBreakStart ? calandar_day.lunchBreakStart|date('H:i:s') : '' }}</td>
</tr>
<tr>
	<th>LunchBreakEnd</th>
	<td>{{ calandar_day.lunchBreakEnd ? calandar_day.lunchBreakEnd|date('H:i:s') : '' }}</td>
</tr>
<tr>
	<th>today's TimeSlots :
	</th>
	<td>
		{% set time_slots = calandar_day.timeSlots %}
		{% if role == "ROLE_USER" %}
			{{ include( 'time_slot/index.html.twig' , {'calandarId': calandar_day.id }   ) }}
		{% else %}
			{{ include( 'time_slot/index.html.twig'  ) }}
		{% endif %}
	</td>

</tr></tbody></table>{% if role == "ROLE_USER" %}<a href="{{ path('app_calandar_access_day_index',{'id': calandar_day.doctor} ) }}">
<button class="btn btn-success">back to list</button></a>{% else %}<a href="{{ path('app_calandar_day_index') }}">back to list</a><a href="{{ path('app_calandar_day_edit', {'id': calandar_day.id}) }}">
<button class="btn btn-success">edit</button></a>{{ include('calandar_day/_delete_form.html.twig') }}{% endif %}{% endblock %}
