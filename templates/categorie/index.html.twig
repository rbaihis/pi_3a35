{% extends 'produitback.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <h1>Categories</h1>
<div class="container-fluid pt-8 px-8">
    <div class="row g-8">
        <div class="col-sm-12 col-xl-8">
            <div class="bg-light rounded h-150 p-8">
                <h6 class="mb-8">Tables des catégories</h6>
                <table class="table">
                    <thead>
            <tr>
                <th>Id</th>
                <th>Nom_categorie</th>
                <th>produits</th>


                <th>actions</th>
            </tr>
        </thead>
        <tbody>

        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.id }}</td>
                <td>{{ categorie.nomCategorie }}</td>
                <td>{{ categorie.getNbProduits() ~ ' ' ~ 'produits'}}</td>
                <td>    {{ include('categorie/_delete_form.html.twig') }}</td>

                <td><a  href="{{ path('app_categorie_show', {'id': categorie.id}) }}" class="btn btn-sm btn-primary" href="">Show</a></td>
                <td><a  href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-sm btn-primary" href="">EDIT </a></td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
                </table>
            </div>
        </div>

                <h1><a  href="{{ path('app_categorie_new') }}" class="btn btn-sm btn-primary" href="">Ajouter </a></h1>

                {% endblock %}
