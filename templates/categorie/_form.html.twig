{{ form_start(form ,{'attr':{'novalidate':'novalidate'}})}}

<div class="col-sm-12 col-xl-6">
    <div class="bg-light rounded h-100 p-4">
        <form>
            <div class="row mb-3">
                {{form_label(form.nom_categorie,'Nom Categorie : ',{'attr':{'class':'form-label'}})}}
                {{form_widget(form.nom_categorie,{'attr':{'class':'form-control'}})}}
                {{form_errors(form.nom_categorie)}}

            </div>

        </form>
    </div>
    <button class="btn btn-sm btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}