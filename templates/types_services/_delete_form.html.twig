<form method="post" action="{{ path('app_types_services_delete', {'id': types_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_service.id) }}">
    <button class="btn">Delete</button>
</form>
