<!DOCTYPE html>

<title>New User</title>

{% block body %}
    <h1>Create new User</h1>

    {{ include('user_info/_form.html.twig') }}

    <a href="{{ path('app_user_info_index') }}">back to list</a>
{% endblock %}
